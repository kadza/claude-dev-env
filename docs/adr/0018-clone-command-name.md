# Command is named `clone`

The command is named `clone` (`clone.sh` in the repo root), matching the `seed`/`unseed`/`cc` verb style. Rejected: `seed-clone` (self-documenting but longer); `graft` (distinctive but opaque). The mild naming collision with `git clone` is acceptable given the verb-style consistency with the rest of this repo's commands.
